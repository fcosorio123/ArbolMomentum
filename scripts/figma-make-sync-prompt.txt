ONE-WAY SYNC ONLY — read GitHub into this Figma Make project.
Do NOT push, commit, or write anything to GitHub. Do NOT create a repository.

TARGET (public repo):
- fcosorio123/ArbolMomentum
- Branch: main
- Pin commit (verify after sync): 2842f085bd76317a18572122285b6da99977f92a
- Browse: https://github.com/fcosorio123/ArbolMomentum

=== SYNC STRATEGY (use in order; do not give up after one failure) ===

STEP 1 — Try GitHub API (max 3 attempts, wait 15s between retries)
- Fetch tree from fcosorio123/ArbolMomentum @ main
- If you get 520, 502, 503, timeout, or "blocked": note the error and go to STEP 2 (do NOT stop)

STEP 2 — Public HTTP fallback (no PAT required; bypasses API)
Use ANY of these that work in your environment:
A) ZIP download URL:
   https://github.com/fcosorio123/ArbolMomentum/archive/refs/heads/main.zip
   Download → extract → copy files into this project
B) Raw file base URL (per file):
   https://raw.githubusercontent.com/fcosorio123/ArbolMomentum/main/<path>
   Example: https://raw.githubusercontent.com/fcosorio123/ArbolMomentum/main/package.json

STEP 3 — If both fail, sync critical paths only via raw URLs (batch by folder):
- package.json, package-lock.json, vite.config.ts, index.html
- public/ (entire folder)
- src/ (entire folder)
- .figma/make/
- supabase/functions/server/ (if present)

=== FILES THAT MUST MATCH GITHUB main ===
src/
public/
package.json
package-lock.json
vite.config.ts
index.html
.figma/make/
supabase/ (if in repo)

=== STRICT RULES ===
- GitHub wins on every conflict
- Do NOT Push to GitHub
- Do NOT Create repository
- Do NOT commit to GitHub
- Ignore Make-only orphans (e.g. image-13.png) if not on GitHub main
- Do NOT ask me to upload a ZIP unless STEP 2A download also failed

=== VERIFY BEFORE SAYING DONE ===
1. Confirm which method worked (API, ZIP URL, or raw files)
2. Show first line of package.json "name" field from synced copy
3. Confirm public/sw.js exists and contains CACHE_NAME = 'arbol-v4'
4. List top-level folders you updated

Then remind me: Publish → Update (top-right), then hard-refresh figma.site.
